{% extends 'fr/base.html.twig' %}
{# Modele de la page contact #}
	{% block body %}
    <div class="container">
           <div class="row">
               <div class="col-sm-4"  style="margin-left:auto; margin-right:auto;">
                   <h2 class=page-header>Contactez nous</h2>
                       {{form_start(form)}}
                       {{form_widget(form)}}
                       {{form_end(form)}}
               </div>
           </div>
    </div>
	{% endblock %}
